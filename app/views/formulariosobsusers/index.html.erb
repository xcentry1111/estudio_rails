<p id="notice"><%= notice %></p>

<h1>Formulariosobsusers</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @formulariosobsusers.each do |formulariosobsuser| %>
      <tr>
        <td><%= link_to 'Show', formulariosobsuser %></td>
        <td><%= link_to 'Edit', edit_formulariosobsuser_path(formulariosobsuser) %></td>
        <td><%= link_to 'Destroy', formulariosobsuser, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Formulariosobsuser', new_formulariosobsuser_path %>
